// 遊戲配置
export const GRID_SIZE = 20; // 每個格子的像素大小
export const TILE_COUNT = 20; // 遊戲區域的格子數量 (20x20)
export const CELL_SIZE = GRID_SIZE; // CELL_SIZE 是 GRID_SIZE 的別名

// 方向定義
export const DIRECTIONS = {
  UP: 'up',
  DOWN: 'down',
  LEFT: 'left',
  RIGHT: 'right',
};
